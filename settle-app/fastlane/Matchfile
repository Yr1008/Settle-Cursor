git_url(ENV['MATCH_GIT_URL'])

storage_mode("git")
type("appstore")

app_identifier(["com.example.settle"])
username(ENV['APPLE_ID'])

git_branch(ENV['MATCH_GIT_BRANCH'] || "main")
keychain_name(ENV['MATCH_KEYCHAIN_NAME'] || "fastlane_tmp_keychain")
keychain_password(ENV['MATCH_KEYCHAIN_PASSWORD'] || "fastlane")
